# 작업 세션 — 2026-04-08

## 목표
오랫동안 중단된 kw4_x (cocos2d-x 한글 단어카드 게임) Windows 빌드 환경 구성 및 실행

## 프로젝트 정보
- 엔진: cocos2d-x 3.17.2
- 타겟: Windows (VS2022) / iOS
- 기존 상태: iOS 프로젝트만 존재, Windows 프로젝트 없음, external 라이브러리 미설치

---

## 작업 내용

### 1. Windows 빌드 환경 구성
- `kw4_x/CMakeLists.txt` 생성 (CMake + VS2022)
- `kw4_x/proj.win32/` 생성: `main.cpp`, `main.h`, `resource.h`, `game.rc`, `res/game.ico`
- `download_deps_py3.py` 작성: cocos2d-x external 라이브러리 GitHub에서 다운로드 (134MB)
  - `python3 download_deps_py3.py` 로 실행
- `.gitignore` 생성

### 2. 빌드 명령
```bash
cd d:/MyDoc/Rebuild/kw4_x/kw4_x
cmake -B build_win32 -G "Visual Studio 17 2022" -A Win32 .
cmake --build build_win32 --config Debug --target kw4_x -j4
```
실행 파일: `build_win32/bin/kw4_x/Debug/kw4_x.exe`

### 3. 코드 버그 수정

#### common_define.h — gettimeofday() Windows 미지원
```cpp
// 수정 전
gettimeofday(&tv, 0);
// 수정 후
#if CC_TARGET_PLATFORM == CC_PLATFORM_WIN32
    return (unsigned long)GetTickCount();
#else
    gettimeofday(&tv, 0);
#endif
```

#### wordFactory.cpp — UTF-8 이중 변환 버그
- `word_factory.txt`는 UTF-8인데 `UTF8()` 함수(CP949→UTF-8 변환)를 호출해 데이터 깨짐
- `UTF8()` 호출 제거

#### 한글 폰트 — createWithSystemFont("Arial") 한글 미지원
- `C:/Windows/Fonts/malgun.ttf` → `Resources/fonts/malgun.ttf` 복사 (직접 복사 필요, 저작권 이슈로 git 미포함)
- `stdafx.h`에 `#define KR_FONT_TTF "fonts/malgun.ttf"` 추가
- `createWithSystemFont(..., "Arial", ...)` → `createWithTTF(..., KR_FONT_TTF, ...)` 변경
  - `TextLayer.cpp:44`
  - `StudyScene.cpp:487`
  - `TouchedHandleLayer.cpp:215`

### 4. 소스 파일 인코딩 변환 (CP949 → UTF-8 BOM)
다음 8개 파일 변환:
- AppDelegate.cpp, AppleTouchedHandleLayer.cpp, AppleTreeScene.cpp
- InfoScene.cpp, MainMenuScene.cpp, PointManager.cpp
- SoundFactory.h, UI_GameResultWindow.cpp

### 5. GitHub Push
커밋: `b4cc11a` — "Add Windows build support and fix Korean text rendering"

---

## 다음 목표
- Mac에서 iOS 빌드 시도
- `proj.ios_mac/ios/` Xcode 프로젝트 또는 CMake iOS 빌드

## 환경 참고
- `python` = Python 2.7 (C:/Python27/), `python3` = Python 3.7.9
- Visual Studio 2022 Professional
- NVIDIA RTX 2070 / OpenGL 4.6
